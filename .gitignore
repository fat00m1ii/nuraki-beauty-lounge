# dependencies
node_modules
.pnp
.pnp.js

# build
dist
dist-ssr

# env — never commit secrets (Supabase / ImageKit / Resend keys)
.env
.env.local
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / os
.DS_Store
.idea
.vscode
*.local

# raw originals (served via ImageKit / optimized copies live in public/)
_source_assets
.vercel
.env*
